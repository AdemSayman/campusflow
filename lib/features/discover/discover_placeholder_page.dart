import 'package:flutter/material.dart';

class DiscoverPlaceholderPage extends StatelessWidget {
  const DiscoverPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Keşfet — yakında'),
    );
  }
}
